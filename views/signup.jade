extends layout

block content
    .container-narrow
        .jumbotron
            h2 How Swift Are You?
            p.
                SwiftCODE is a multiplayer typing speed game for programmers.
                Just choose a username and password to begin!
                Or, if you don't care about saving progress,
                <a href="/playnow"><span class="label label-success">play now!</span></a>
            if error.length != 0
                .alert.alert-danger
                    button.close(type='button', data-dismiss='alert') x
                    strong Oops! 
                    = error
            form.form-signup.form-parsley(action='/create-account', method='POST')
                .form-group
                    input.form-control.form-username(type='text', placeholder='Username', name='username', data-required='true', data-rangelength='[2, 32]', data-placement='right')
                .form-group
                    input.form-control.form-password(type='password', placeholder='Password', name='password', data-required='true', data-minlength='8', data-placement='right')
                .form-group
                    input.form-control.form-repeatpassword(type='password', placeholder='Repeat Password', data-required='true', data-equalto='.form-password', data-placement='right')
                button.btn.btn-large.btn-primary.btn-block(type='submit') Sign up
